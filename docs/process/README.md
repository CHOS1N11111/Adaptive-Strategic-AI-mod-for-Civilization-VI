# 开发过程资料

> 分类：开发过程 | [返回文档索引](../README.md)

本目录用于保存开发过程记录，不承载产品目标或正式设计结论。

- `DEVELOPMENT.log`：按时间追加的本机工作记录，通过仓库的 `*.log` 规则排除，不进入 Git。
- `session-data/`：容易被游戏下次启动覆盖的原始日志与端点存档，仅作本机复核，通过目录规则排除，不进入 Git。
- 需要长期保留并参与评审的结论，应整理到 `product`、`research`、`design` 或 `quality` 对应文档，而不是只写在日志中。
